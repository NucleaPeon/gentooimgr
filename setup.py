from setuptools import find_packages, setup

setup(
    name='gentooimgr',
    description='Gentoo image generator and install automator tool',
    version='0.2.0',
    include_package_data=True,
    packages=find_packages()
)
